#include "util.h"
long	dec_to_hex_length(long n)
{
  int l;

  l = 0;
  while (nb / 16)
    l++;
  return (l);
}
char	*dec_to_hex(long n)
{
  int nb;

  nb = n;
  while (nb--)
    {
      
      nb / 16;
    }
}
